"""Cortesol — agent memory with an epistemic immune system for peptides research.

The LLM proposes; the ledger disposes. Belief lives in an explicit graph and moves
only by capped, provenance-carrying log-odds increments — never because text said so.

See docs/ARCHITECTURE.md for how it works and docs/COMPONENTS.md for how the code
is split. The filled-in files under core/ are the shared contracts; most other
files are TODO stubs.
"""

__version__ = "0.1.0"
