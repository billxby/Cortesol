# Freesolo Flash — SFT config (area B). TODO: fill in real values at kickoff.
# Reference: Fine-Tuning Plan §Stage 1. Validate on 0.8B, train for real on 4B.

[model]
base = "Qwen/Qwen3.5-4B"      # validate the pipeline on 0.8B first
thinking = true               # SFT targets include <think>…</think> short rationale

[train]
algorithm = "sft"
# lora_rank = 32
# learning_rate = 1e-4
# max_steps = 500
# max_completion_tokens = 512
# save_every = 100
# seed = 0

# structured_outputs = op schema (ops.ops_json_schema()) — constrains decoding
[train.structured_outputs]
# schema = "path/to/ops_schema.json"   # TODO: dump ops_json_schema() here

[data]
# train = "data/sft/train.jsonl"       # rows {input, output, metadata}
# eval  = "data/sft/eval.jsonl"

[wandb]
# project = "cortesol-sft"
