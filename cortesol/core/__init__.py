"""Core = the shared contracts + the deterministic belief engine (logical area A).

Filled in (the vocabulary everything imports): schema, ops, domain, config, mathx,
context, results, kb. TODO (the engine logic): engine, validator, propagate.
"""
