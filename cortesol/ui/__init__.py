"""UI (area C) — the live belief-graph demo. FastAPI + SSE streaming to a
vis-network graph: node color = confidence (red->green), size = downstream impact,
pulse on update; side panel = event feed + audit log. Graph Propagation §5.
"""
